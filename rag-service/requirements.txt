# Core
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.0
pydantic-settings==2.3.0
python-dotenv==1.0.0

# LLM & Embeddings
openai==1.35.0              # text-embedding-3-small + GPT-4o-mini

# Vector Store
pymilvus==2.4.0             # Phase 1
# pinecone-client==4.0.0    # Phase 2 — uncomment when migrating

# Sparse Retrieval
rank-bm25==0.2.2

# Reranking
sentence-transformers==3.0.0   # cross-encoder/ms-marco-MiniLM-L-6-v2

# Tokenization (chunker)
tiktoken==0.7.0
nltk==3.8.1

# Data & Ingestion
pandas==2.2.0
psycopg2-binary==2.9.9
aiokafka==0.11.0            # async Kafka consumer (replaces kafka-python)
redis==5.0.4

# Evaluation
# ragas==0.1.9    # NOT installed on purpose — eval/faithfulness_judge.py falls back to
#                 # its own two-step GPT-4o-mini judge, which is what the locked baseline
#                 # was measured with. Installing ragas silently switches judges and makes
#                 # faithfulness incomparable to eval/baseline_metrics.json.
locust==2.28.0

# Utilities
httpx==0.27.0
tenacity==8.3.0             # retry logic for OpenAI API calls
numpy==1.26.4
ftfy==6.2.0                 # fixes mojibake / encoding corruption in review text

# Dev / test
pytest==8.2.0
pytest-asyncio==0.23.7
